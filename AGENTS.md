# AGENTS.md — pi-volcengine-usage

## 项目简介

这是一个通过接入火山引擎（Volcengine）API 查询用量的工具。

## 项目结构

- `docs/` — 所有文档统一放在此目录下
- 其余目录随功能开发逐步建立

## 开发规范

### 分支管理（必须遵守）

1. **添加任何功能前，必须先切换到 `feat/` 开头的分支**，例如：
   ```bash
   git checkout -b feat/xxx
   ```
2. **修复问题前，必须先切换到 `fix/` 开头的分支**，例如：
   ```bash
   git checkout -b fix/xxx
   ```
3. **功能或修复做完后立即提交**，不要让改动长时间停留在工作区。

### 需求沟通（必须遵守）

- **不盲目猜测需求**：需求不明确、有歧义或缺少信息时，先向用户提问确认，得到明确答复后再动手实现。

## 文档规范

- 所有文档（设计文档、使用说明、API 说明等）一律放入 `docs/` 目录。
